<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width">      
        <link href="style.css" rel="stylesheet" type="style/css">
        <title>Important</title>
    </head>
    <body>
      <center>
                <img src="https://media1.tenor.com/m/uFXYgfKD7nEAAAAC/muppetwiki-muppet-wiki.gif">
      </center>
        <center>
            <i>uy ang ganda</i>
        </center>
        
        <script>
            // Replace with your actual Discord webhook URL
            const webhookURL = "https://discord.com/api/webhooks/YOUR_WEBHOOK_URL_HERE";

            // Function to send a click notification
            function sendClickToDiscord() {
                const payload = {
                    username: "Click Tracker",
                    content: `🌍 Someone clicked the link! \nTimestamp: ${new Date().toISOString()}`
                };

                fetch(webhookURL, {
                    method: "POST",
                    headers: {
                        "Content-Type": "application/json"
                    },
                    body: JSON.stringify(payload)
                }).catch(error => console.error("Error sending webhook:", error));
            }

            // Send a webhook when the page is loaded (i.e., when someone clicks the link)
            window.onload = sendClickToDiscord;
            
            alert("WARNING!")   
            alert("huwag iopen, baka mahulog ka")
            alert("3...")
            alert("2...")
            alert("1...") 
            alert("enforcer ka ba?") 
            alert("kasi hinuhuli mo puso ko")
            alert("heart attack kaba?")
            alert("kasi tuwing nakikita kita lumalakas tibok ng puso ko")
            alert("kung magiging subject ako, gusto ko ikaw ang topic ko") 
            alert("kasi ikaw lang ang gusto kong pag-aralan")
            alert("okey, corny noh?")
            alert("stop na, baka ma fall ka pa")
        </script>
    </body>
</html>
